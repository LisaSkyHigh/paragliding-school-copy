# Copy to `.github/workflows/deploy-github-pages.yml` when your Git token has the `workflow` scope.
# GitHub OAuth apps cannot push workflow files without it; see docs/CHECKPOINT-rollback-and-site-map.md.

name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-paragliding-school
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Remove routes incompatible with static export
        run: rm -rf "app/(studio)" "app/preview"

      - name: Build static site
        env:
          STATIC_GH_PAGES: "1"
          NEXT_PUBLIC_SITE_URL: https://lisaskyhigh.github.io/paragliding-school
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4
